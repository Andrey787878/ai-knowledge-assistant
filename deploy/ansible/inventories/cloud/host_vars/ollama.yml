---
# Expose Ollama on private network only (firewall-guarded)
ollama_bind_host: "{{ private_ip }}"

firewall_ssh_sources:
  - "{{ hostvars['wiki'].private_ip }}/32"

firewall_rules:
  - port: 11434
    proto: tcp
    source: "{{ hostvars['n8n'].private_ip }}/32"
